fun hello(name) {
    return "Hello, " + name + "!";
}

fun count(n) {
    if (n > 1) count(n - 1);
    print n;
}

print hello("World");
print "The current machine time in seconds is: " + clock();
print "I'll start counting now:";
count(3);

